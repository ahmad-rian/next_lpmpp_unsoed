# ============================================
# LPMPP UNSOED - Environment Variables
# ============================================
# Copy file ini ke .env dan isi dengan nilai yang sesuai
# cp .env.example .env

# ============================================
# DATABASE
# ============================================
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# Untuk production, gunakan database yang sudah dibuat
DATABASE_URL="mysql://username:password@localhost:3306/lpmpp_unsoed"

# ============================================
# NEXTAUTH / AUTH.JS
# ============================================
# URL aplikasi (tanpa trailing slash)
# Development: http://localhost:3000
# Production: https://lpmpp.unsoed.ac.id
NEXTAUTH_URL="https://lpmpp.unsoed.ac.id"

# Secret key untuk enkripsi session (wajib di production)
# Generate dengan: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-key-min-32-characters"

# Trust host untuk production (Next-Auth v5)
AUTH_TRUST_HOST=true

# ============================================
# GOOGLE OAUTH
# ============================================
# Dapatkan dari Google Cloud Console:
# https://console.cloud.google.com/apis/credentials
#
# Authorized JavaScript origins:
#   - https://lpmpp.unsoed.ac.id
#
# Authorized redirect URIs:
#   - https://lpmpp.unsoed.ac.id/api/auth/callback/google
#
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ============================================
# ADMIN CONFIGURATION
# ============================================
# Email admin yang diizinkan login (opsional, bisa diatur via database)
ADMIN_EMAIL="admin@unsoed.ac.id"

# ============================================
# OPTIONAL - NODE ENVIRONMENT
# ============================================
# Untuk production, pastikan NODE_ENV=production
NODE_ENV="production"
